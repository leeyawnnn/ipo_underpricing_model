# Runtime dependencies for the analysis pipeline.
# Lower bounds only: the CI matrix runs Python 3.11, 3.12 and 3.13, and hard
# pins to a single patch release cannot satisfy all three.
# requirements-dev.txt adds the lint/type/test toolchain.

# Core data
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=14

# HTTP / scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Finance data
yfinance>=0.2.36

# ML
scikit-learn>=1.3,<2
lightgbm>=4.3    # macOS also needs OpenMP: brew install libomp
shap>=0.44

# Visualisation
matplotlib>=3.8

# Statistics
scipy>=1.11
statsmodels>=0.14
