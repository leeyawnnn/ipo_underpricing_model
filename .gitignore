# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Environment
.env
.venv/
venv/
env/

# macOS
.DS_Store
.AppleDouble
.LSOverrideN

# Data — large raw files (>50 MB rule: keep CSVs, ignore parquet blobs and raw HTML dumps)
data/raw/s1_filings/*.txt
data/raw/s1_filings/*.html
# Keep small external reference files; ignore large market dumps
data/raw/*.html

# Optuna study databases
*.db
optuna_study.sqlite3

# Model artefacts
*.pkl
*.joblib
models/

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db

*.pdf
# Scrape caches — regenerated by the scrapers, not analysis inputs.
# Tracking these made GitHub report the repo's language as HTML.
data/raw/.cache/

# Fetched reference data (see scripts/fetch_lm_dictionary.py)
data/external/lm_master_dictionary.csv

# Generated reports: the tables and their provenance sidecars are committed,
# because the README quotes them and a reader must be able to trace a number
# back to the commit and input hash that produced it.

# Coverage
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Superseded by data/external/lm_sentiment_words.csv (derived subset).
data/external/lm_dictionary.csv

# Fetched by scripts/fetch_underwriter_ranks.py
data/external/ritter_underwriter_rank.xls

# stockanalysis.com's IPO calendar is their compilation. Their terms permit
# snippets with attribution but not republishing content in full, so the raw
# scrape stays local and is regenerated by src/scraper_ipo_calendar.py.
# The derived analysis dataset in data/processed/ is committed.
data/raw/ipo_calendar.csv
data/raw/ipo_master_raw.csv
data/raw/prices.csv
data/raw/first_day_prices.csv
data/interim/
data/processed/ipo_analysis.parquet
data/raw/edgar_scrape_summary.csv
